{
  "name": "englishgo",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:novel-audio": "node scripts/generateNovelAudioCatalog.mjs",
    "prewarm:novel-audio": "node scripts/prewarmNovelAudio.mjs",
    "generate:recent-features": "node scripts/recentFeatures.mjs",
    "generate:themed-review-seed": "node scripts/generateThemedReviewSeed.mjs",
    "build": "npm run generate:novel-audio && vite build",
    "build:refresh-features": "npm run generate:recent-features && npm run build",
    "preview": "vite preview",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@vitejs/plugin-react": "^4.7.0",
    "jsdom": "^24.1.3",
    "vite": "^6.4.2",
    "vitest": "^3.2.4"
  }
}
